const assert = require('assert');
const mongooseDal = require('../index.js');

describe('mongooseDal', function () {
  it('has a test', function () {
    assert(false, 'mongooseDal should have a test');
  });
});
